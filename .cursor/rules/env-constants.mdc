---
description: 项目环境常量定义：.env 中所有变量及用途，用于 config/projects、config/default、换机配置与 AI 上下文。
globs: "config/**/*.ts,.env.example,.env"
alwaysApply: false
---

# 环境常量（.env）
<!-- AI 生成 By Peng.Guo -->

所有可配置的路径、Jenkins 任务名、分支、固定网址均通过 `.env` 提供，复制 `.env.example` 为 `.env` 后按本机修改。换电脑或换环境时只需改 `.env`，无需改代码。

**AI 改配置**：按 `.cursor/rules/env-direct-edit.mdc`，用户提出调参/超时/模型等需求时**直接改 `.env`**，并同步 `.env.example` 与相关文档，无需每次征求确认。

## 一、固定网址与目录（config/default.ts）

| 变量 | 含义 | 示例/默认 |
|------|------|-----------|
| `OLLAMA_BASE` | Ollama 服务地址 | `http://localhost:11434` |
| `OLLAMA_MODEL` | 默认模型名 | `qwen2.5:3b` |
| `OLLAMA_AGENT_TEMPERATURE` | Chat Agent 工具路由 temperature，宜 `0` 减少同指令偶发不调 tool | `0` |
| `OLLAMA_KB_TEMPERATURE` | 知识库问答/预处理/重排 temperature，宜偏低 | `0.1` |
| `OLLAMA_WEEKLY_REPORT_TEMPERATURE` | 写周报生成 temperature | `0.4` |
| `OLLAMA_TEAM_SUMMARY_TEMPERATURE` | 组内总结生成 temperature | `0.4` |
| `OLLAMA_TECH_DIGEST_TEMPERATURE` | 技术趋势页签 digest 分析 temperature | `0.3` |
| `TECH_DIGEST_GITHUB_LIMIT` | GitHub Trending 抓取条数 | `50` |
| `TECH_DIGEST_HN_LIMIT` | Hacker News Top 条数 | `30` |
| `TECH_DIGEST_REDDIT_LIMIT` | 每个 Reddit 版块 Top 条数 | `20` |
| `TECH_DIGEST_FETCH_TIMEOUT_MS` | 单数据源抓取超时（毫秒） | `30000` |
| `TECH_DIGEST_LLM_TIMEOUT_MS` | digest LLM 分析总超时（毫秒） | `600000` |
| `GITHUB_TOKEN` | 可选：GitHub API Token，提升 repo 元数据质量 | 留空 |
| `REDDIT_USER_AGENT` | Reddit 抓取 UA（建议 `desktop:app:version (by /u/你的用户名)`） | 内置默认 |
| `REDDIT_CLIENT_ID` | 可选：Reddit App Client ID，配置后优先走 OAuth JSON API | 留空 |
| `REDDIT_CLIENT_SECRET` | 可选：与 Client ID 配对的 Secret | 留空 |
| `TECH_DIGEST_REDDIT_GAP_MS` | 串行 Reddit 请求最小间隔（毫秒），6 次抓取约需 48s+ | `8000` |
| `TECH_DIGEST_REDDIT_429_COOLDOWN_MS` | Reddit 返回 429 后的全局冷却（毫秒） | `45000` |
| `TECH_DIGEST_REDDIT_FETCH_TIMEOUT_MS` | Reddit 单版块抓取总超时（含限流重试，毫秒） | `180000` |
| `OLLAMA_THINK` | 是否在 `/api/chat` 传 `think`；**默认不传**（兼容非 thinking 模型）；`true`/`1`/`low`/`medium`/`high` 开启；若开启而模型不支持，会自动去掉 `think` 重试 | 留空 |
| `API_PORT` | 本应用 API 端口（**勿用 `PORT`**，cc-web cjet dev 会读 `PORT` 抢端口） | `41738` |
| `SHELL_CWD` | Shell 工具允许的工作目录 | 空则用 `process.cwd()` |
| `MACOSTUNMODE_DIR` | 「tun」指令：macostunmode 控制器目录 | 空则用仓库根目录下 `macostunmode` |
| `MACOSTUNMODE_SUDO_PASSWORD` | 「tun」指令：sudo 密码；留空则在系统终端手动输入 | 留空 |
| `JENKINS_BASE_URL` | Jenkins 根地址 | `https://jenkins.rd.chanjet.com/` |
| `JENKINS_USERNAME` | Jenkins Basic 认证用户名 | - |
| `JENKINS_TOKEN` | Jenkins API Token（Basic 密码） | - |
| `JIRA_BASE_URL` / `JIRA_USERNAME` / `JIRA_PASSWORD` | Jira 8.8 Basic 认证 | - |
| `JIRA_SUBMIT_TEST_TRANSITION_NAME` | 列表一键提测工作流名称 | `提测` |
| `JIRA_SUBMIT_TEST_CAN_GRAYSCALE` | 一键提测「能否灰度」选项文案 | `能` |
| `JIRA_SUBMIT_TEST_MODIFICATION_TEMPLATE` | 修改点模板（`{摘要}` 占位） | `修复：{摘要}` |
| `JIRA_SUBMIT_TEST_SCOPE_TEMPLATE` | 测试范围建议模板 | `验证：{摘要}` |
| `JIRA_SUBMIT_TEST_DEFECT_CAUSE` | 可选缺陷原因选项；不匹配则跳过 | 留空 |
| `GEMINI_API_KEY` / `GOOGLE_API_KEY` | 外部 Gemini 鉴权（与 A2UI 一致）；未在 UI 传 Key 时由 `agent/gemini-client.ts` 经 `@google/genai` 读取 | `export GEMINI_API_KEY=...` |
| `GEMINI_DEFAULT_MODEL` | 外部 Gemini 默认模型 ID（UI 未指定 model 时与后端 fallback 一致） | `gemini-3.1-flash-lite` |
| `HTTPS_PROXY` / `HTTP_PROXY` / `ALL_PROXY` | 可选：访问 Google 时代理。`http(s)://` 走 undici `ProxyAgent`；**`socks5://` / `socks5h://`** 走 `fetch-socks`（undici 不支持 socks URL） | 例：`http://127.0.0.1:7890` 或 `socks5h://127.0.0.1:1081` |
| `GEMINI_SOCKS_URL` | 可选：仅 SOCKS 时代理地址，优先级高于 `ALL_PROXY` 等 | 例：`socks5://127.0.0.1:1081` |
| `GEMINI_USE_MAC_SYSTEM_SOCKS` | 默认自动：在 macOS 且未配置上述 HTTP/SOCKS 环境变量时，读取 `scutil --proxy` 的系统 SOCKS；设为 `0` 可关闭 | `0` |
| `GEMINI_CONNECT_TIMEOUT_MS` | undici **TCP 连接**超时（毫秒），默认 `120000`；仅作用于外部 Gemini 请求 | `180000` |

## 一点五、知识库（config/default.ts → knowledgeBase）

| 变量 | 含义 | 默认 |
|------|------|------|
| `KB_QUERY_TIMEOUT_MS` | 单次知识库查询超时（毫秒） | `300000`（5 分钟） |
| `KB_CHAT_MODEL` / `KB_EMBED_MODEL` / `KB_INGEST_MODEL` 等 | 见 `.env.example`「知识库」段 | - |

## 二、Jenkins 默认分支（config/projects.ts → 部署时 BRANCH_NAME）

| 变量 | 用途 |
|------|------|
| `JENKINS_DEFAULT_BRANCH_PRETEST` | 预发/测试线项目部署默认分支（如 test-260423） |
| `JENKINS_DEFAULT_BRANCH_NOVA` | Nova 项目部署默认分支（如 test） |

## 三、合并目标分支（config/projects.ts → merge 接口）

| 变量 | 用途 |
|------|------|
| `MERGE_TARGET_BRANCH_PRETEST` | 预发线项目合并目标分支 |
| `MERGE_TARGET_BRANCH_NOVA` | Nova 项目合并目标分支 |

## 四、Jenkins 任务名（config/projects.ts，与 Jenkins /job/ 后一致）

| 变量 | 对应项目代号 |
|------|--------------|
| `JENKINS_JOB_BIZ_GUIDE` | biz-guide |
| `JENKINS_JOB_BIZ_SOLUTION` | biz-solution |
| `JENKINS_JOB_REACT18` | react18 |
| `JENKINS_JOB_BASE` | base |
| `JENKINS_JOB_BASE18` | base18 |
| `JENKINS_JOB_SCM` | scm |
| `JENKINS_JOB_CC_WEB` | cc-web |
| `JENKINS_JOB_CC_NODE` | cc-node |
| `JENKINS_JOB_NOVA` | nova / nova-next |
| `JENKINS_JOB_MDF_UI` | mdf-ui |
| `JENKINS_JOB_MDF_BIZ` | mdf-biz |
| `JENKINS_JOB_MDF_REPORT` | mdf-report |

## 五、项目路径（config/projects.ts）

变量名规则：`PROJECT_PATH_<代号>`，代号中的 `-` 写成 `_`（如 biz-guide → `PROJECT_PATH_BIZ_GUIDE`）。

- 每个项目一条，与 `config/projects.ts` 中 `ProjectEntry.codes` 对应。
- 路径可分散在不同盘/不同目录，无统一前缀要求。
- 完整列表见 `.env.example` 中 “项目路径” 段。

### 项目开发启动命令（可选）

| 变量 | 用途 |
|------|------|
| `PROJECT_DEV_CMD_MDF_UI` | mdf-ui 的「启动 mdf-ui」命令，默认 `yarn w` |
| `PROJECT_DEV_CMD_MDF_BIZ` | mdf-biz 的「启动 mdf-biz」命令，默认 `yarn w` |
| `PROJECT_DEV_CMD_MDF_REPORT` | mdf-report 的「启动 mdf-report」命令，默认 `pnpm run dev` |

未配置时，工作流外项目默认 `yarn dev`；mdf-ui / mdf-biz 在 `config/projects.ts` 中已默认 `yarn w`；mdf-report 默认 `pnpm run dev`。

## 使用约定

- 修改或新增项目时：在 `config/projects.ts` 增加/调整条目，并在 `.env.example` 与本文档中同步变量名与说明。
- 用户要求改环境参数时：**直接改 `.env`**（见 `env-direct-edit.mdc`），并同步 `.env.example`、本文档；不要只改示例却让用户自己抄到 `.env`。
- 代码中仅通过 `process.env.XXX` 或 `config/default.ts` 的 `config` 读取，不写死路径/网址/任务名。
